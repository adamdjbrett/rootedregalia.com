    <section class="bulk-hero" style="background: linear-gradient(rgba(40,54,24,0.8), rgba(40,54,24,0.7)), url('{{image}}') no-repeat center/cover;
            min-height: 300px;
            display: flex;
            align-items: center;
            color: var(--cream);">
        <div class="container bulk-hero-content text-center">
            <h1 class="display-5 fw-bold">{{title}}</h1>
            <p class="lead mt-3">{{description}}</p>
        </div>
    </section>
